apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: todo-ms
  labels:
    app: task-db
  name: task-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: task-db
  strategy: {}
  template:
    metadata:
      labels:
        app: task-db
    spec:
      containers:
      - image: postgres:17.2-alpine3.20
        name: postgres
        resources: {}
        env:
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: todo-secret
                key: TASK_DB_PASSWORD
          - name: POSTGRES_USER
            valueFrom:
              configMapKeyRef:
                name: todo-configmap
                key: TASK_DB_USER
          - name: POSTGRES_DB
            valueFrom:
              configMapKeyRef:
                name: todo-configmap
                key: TASK_DB_NAME
status: {}

---


apiVersion: v1
kind: Service
metadata:
  namespace: todo-ms
  labels:
    app: task-db
  name: task-db
spec:
  ports:
  - port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: task-db
  type: ClusterIP
status:
  loadBalancer: {}
